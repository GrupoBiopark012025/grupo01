import { BackOrNavigateToDirective } from './back-or-navigate-to.directive';

describe('BackOrNavigateToDirective', () => {
  it('should create an instance', () => {
    const directive = new BackOrNavigateToDirective();
    expect(directive).toBeTruthy();
  });
});

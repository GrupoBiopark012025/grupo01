import { ControlValidationFeedbackDirective } from './control-validation-feedback.directive';

describe('ControlValidationFeedbackDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlValidationFeedbackDirective();
    expect(directive).toBeTruthy();
  });
});
